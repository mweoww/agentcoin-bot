# ============================================
# AgentCoin 全自动挖矿机器人 - 配置文件
# 复制为 .env 并填写你的配置
# ============================================

# === 钱包 ===
PRIVATE_KEY=               # 留空则自动生成新钱包

# === X (Twitter) API 主通道 ===
X_HANDLE=your_x_username   # X 用户名（不带@）
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# === X Cookie 备用通道（反检测降级） ===
X_AUTH_TOKEN=              # 浏览器 Cookie 中的 auth_token
X_CT0=                     # 浏览器 Cookie 中的 ct0

# === 全局代理（带认证） ===
PROXY_HOST=your_proxy_host:port
PROXY_AUTH=user:pass

# === AI (Claude) ===
ANTHROPIC_BASE_URL=        # Claude API 代理地址（可选）
ANTHROPIC_AUTH_TOKEN=      # Claude API Key

# === 链 (推荐使用 Alchemy/Infura 专用 RPC) ===
BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# === 运行参数 ===
POLL_INTERVAL=30           # 轮询间隔（秒）
AUTO_CLAIM=true            # 自动领取奖励
